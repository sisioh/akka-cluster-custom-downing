akka {
  test.timefactor = ${?TEST_TIME_FACTOR}
  loglevel = DEBUG
  stdout-loglevel = INFO
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"

  log-dead-letters = 10
  log-dead-letters-during-shutdown = on

  actor {
    debug {
      # receive = on
      # lifecycle = on
    }
  }

}